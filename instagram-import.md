# 인스타그램 수집 — 현재 상태

DM으로 들어온 인스타 게시물을 **가고싶어요 / 배달 / 사고싶어요**로 나눠 저장하는 흐름의 정리.
2026-09-10 기준. 여기 적힌 인스타 동작은 전부 프로덕션에서 실측한 것이다.

---

## 흐름

```
인스타 게시물
   ├─ (A) 공유 시트 → "다녀왔어요"      → 링크가 그대로 전달됨      ← 완전 자동
   └─ (B) DM으로 수집 계정에 공유        → 웹훅 → "분류해주세요"
                                              ├─ 릴스: 링크 옴      ← 완전 자동
                                              └─ 캐러셀: 링크 없음  ← 링크 1회 입력
                                                     ↓
                                         등록 폼 (슬라이드 짝짓기 + 이름·분류 자동)
                                                     ↓
                                      가고싶어요 / 배달 / 사고싶어요
```

**A(공유 시트)가 가장 빠른 길이다.** 홈 화면에 앱을 추가해두면 인스타 공유 시트에 뜬다.
복사·붙여넣기가 아예 없어진다. (PWA `share_target` → `/share`)

---

## 무엇이 자동이고 무엇이 손이 가나

| 경로 | 링크 | 슬라이드 | 이름·메모·분류 |
|---|---|---|---|
| 공유 시트 | 자동 | 자동 | 자동 |
| DM — 릴스 공유 | 자동 | 해당 없음(1장) | 자동 |
| DM — 캐러셀 공유 | **1회 입력** | 링크 넣으면 자동 | 자동 |

캐러셀 공유에 링크를 한 번 넣으면 **수집함에 저장되어** 다음부터는 손이 가지 않는다.

---

## 왜 캐러셀은 링크가 없나 (실측)

Meta가 웹훅으로 넘겨주는 데이터에 게시물 주소가 없다. 세 방향을 다 확인했다.

1. **본문에 없음** — 링크 없는 DM 5건 조회, `urlsInText: []`, `instagram.com` 문자열조차 없음
2. **첨부에서 역산 불가** — `asset_id`가 17자리(`18099586598365731`)로 미디어 pk(19자리)와 ID 체계가 다름
3. **첨부 주소를 따라가도 없음** — `lookaside.fbsbx.com/ig_messaging_cdn/`는 이미지를 직접 서빙.
   리다이렉트도 `ig_cache_key`도 없음

릴스(`ig_reel`)는 `payload.url`이 퍼머링크로 오지만, 게시물(`share`/`ig_post`)은 CDN 주소로 온다.

---

## 인스타 비공식 동작 (깨지면 여기부터 의심)

공식 API가 아니라 인스타가 바꾸면 조용히 실패한다. 전부 빈 배열로 폴백하도록 작성돼 있다.

| 항목 | 사실 |
|---|---|
| 캐러셀 슬라이드 | `/p/<code>/embed/` (플레인)에만 서버 렌더됨. `/embed/captioned/`는 JS 껍데기 |
| 임베드 UA | **브라우저 UA**여야 JSON이 옴. 크롤러 UA면 껍데기 |
| 임베드 경로 | 반드시 `/p/<code>/embed/`. `/<사용자명>/p/<code>/embed/`는 껍데기(658KB vs 정상 358KB) |
| 자식→부모 리다이렉트 | **비브라우저 UA**에만 옴 (브라우저 UA면 SPA 껍데기 200). 임베드와 반대 |
| shortcode | 미디어 pk(19자리)의 base64 (`A-Za-z0-9-_`) |

> 배포에서만 실패하던 원인이 3행이었다. 손으로 테스트할 땐 줄인 주소만 써서 놓쳤다.

---

## 주소 두 가지를 섞지 않는다

| 필드 | 내용 | 성질 |
|---|---|---|
| `sourceUrl` | 게시물 퍼머링크 | 안 죽음. 슬라이드를 여기서 가져옴 |
| `thumbnailUrl` / `mediaUrls[]` | DM 첨부 CDN 이미지 | **서명 만료됨** |

**라벨은 "어느 칸에 있었나"가 아니라 "어떤 주소인가"로 정한다.**
CDN 주소는 어디에 있든 "공유된 이미지 열기"로 나온다 — 이 규칙이 깨진 원본 링크 문제를 막는다.

등록 폼에서도 자리를 나눈다: **게시물 링크** 칸은 퍼머링크 전용,
**DM에서 받은 주소**는 아래에 따로 표시(저장은 됨). 한 칸에 섞으면 게시물 주소 넣을 자리가 막힌다.

---

## 슬라이드 ↔ 항목 짝짓기

- 항목 수 == 슬라이드 수 → 순서대로
- 슬라이드가 1~2장 많음 → **표지를 건너뛰고** 순서대로 (표지 + 제품들 + 아웃트로 구성)
- 그 외 → 짝짓지 않음. 행마다 탭해서 직접 고름 (엉뚱한 사진은 없느니만 못하다)

저장할 때 폼이 **이미지 주소를 직접 보낸다**. 링크에서 방금 가져온 것이라 DB에 없기 때문.
서버는 인스타 CDN 호스트인지만 확인하고 받는다.

---

## 이름·분류 자동 채움 (Claude 비전)

슬라이드 이미지에서 제품명·정보·종류·분류를 읽는다. `claude-opus-5`, effort `low`.

- **이미지는 서버가 받아 base64로 보낸다.** 주소를 넘기면 Anthropic 서버가 대신 받는데
  인스타 CDN은 서명된 주소라 외부 페처에게 내주지 않는다
- 종류를 미리 정해주지 않는다 — 향수든 맛집이든 배달이든 이미지를 보고 판단
- 여러 장의 **다수결**로 종류·분류를 정한다 (한 장이 튀어도 안 흔들림)
- 사용자가 적은 값은 덮지 않는다. 예외는 `jomalonelondon` 같은 **핸들 꼴 자리표시자**

프로덕션 실측 결과:
```json
{ "found": true, "brand": "조 말론 런던",
  "name": "우드세이지 앤 씨 솔트", "kind": "item", "category": "향수" }
```

---

## 화면 구성

**탭 4개 + 수신함 배지** — 다섯 개는 폰 가로폭을 넘는다.

```
다녀왔어요                              [분류해주세요 5]   ← 밀리면 진하게
가고싶어요 13 · 다녀온곳 0 · 배달 0 · 사고싶어요 2
```

"분류해주세요"는 같은 목록을 거르는 탭이 아니라 **처리할 일감**이라 제목 옆으로 뺐다.
긴 이름(`배달하고 싶은 곳`)은 등록 화면 제목에서만 쓴다.

배달은 가는 게 아니라 시키는 것이라 방문 기록 대신 **"시켜봤어요" 토글**을 쓰고,
분류도 음식 위주로 따로 뒀다.

---

## 운영

- **배포** = `git push origin main` → Vercel 자동 빌드, 빌드 중 `prisma migrate deploy` 실행
- **필요한 환경변수**: `ANTHROPIC_API_KEY`(비전), `DATABASE_URL`/`DIRECT_URL`,
  `APP_PASSWORD`/`AUTH_SECRET`, `INSTAGRAM_*`
- **매니페스트·아이콘은 로그인 없이 열려 있어야 한다** — 브라우저가 쿠키 없이 가져가므로
  막으면 홈 화면 추가가 안 되고, 그러면 공유 시트에도 안 뜬다
- 로컬에는 DB 주소가 없다(Vercel에만 있음). `npm run`이 이 PC에서 깨져서 직접 실행한다:
  - 테스트 `node --test tests/*.test.ts`
  - 타입체크 `node node_modules/typescript/bin/tsc --noEmit`
  - 빌드 `node node_modules/next/dist/bin/next build`

---

## 남은 것

- [ ] **PWA 설치** — 모바일 크롬 ⋮ → "앱 설치". 이걸 해야 공유 시트 경로가 열린다
- [ ] **`attachmentFields` 로그 확인** — 웹훅이 첨부 payload의 필드 이름을 남긴다.
      캐러셀 공유에 퍼머링크 필드가 있으면 링크 입력마저 없앨 수 있다.
      Vercel → Logs → `[instagram-webhook]` 줄
- [ ] 중복으로 저장된 항목 정리 — 목록에서 **선택 → 전체 선택 → 삭제**
