# 소셜 수집 플랫폼 구축 플레이북

SNS에서 공유받은 것을 내 데이터로 만드는 앱을 지을 때 쓰는 패턴 모음.
visitlog(인스타 DM → 가고싶어요/배달/사고싶어요)를 만들며 실제로 부딪힌 것만 담았다.
유튜브·틱톡·스레드·네이버 블로그·핀터레스트 등 **다른 플랫폼에 옮겨도 그대로 쓰인다.**

읽는 법: 각 항목은 **원칙 → 왜 → 어떻게**다. 원칙만 읽어도 지뢰는 피할 수 있다.

---

## 1. 수집 경로는 항상 셋을 깐다

**원칙.** 한 경로에 의존하면 플랫폼이 데이터를 덜 주는 순간 전부 막힌다.

| 경로 | 신뢰도 | 특징 |
|---|---|---|
| ① OS 공유 시트 (PWA `share_target`) | **가장 높음** | 플랫폼 데이터 형식과 무관. 원본 주소가 그대로 옴 |
| ② 플랫폼 웹훅 / DM | 중간 | 공짜로 들어오지만 **주는 데이터가 종류마다 다르다** |
| ③ 사용자가 주소를 직접 넣기 | 항상 됨 | 마지막 보루. 없애지 말 것 |

**왜.** visitlog에서 Meta 웹훅은 릴스 공유엔 원본 주소를 주고, 캐러셀 게시물엔 안 줬다.
같은 "공유"인데 콘텐츠 종류에 따라 payload가 달랐다. 웹훅만 믿었다면 절반이 죽었다.

**어떻게.**
```json
// manifest.webmanifest — 설치하면 OS 공유 시트에 앱이 뜬다
"share_target": {
  "action": "/share", "method": "GET",
  "params": { "title": "title", "text": "text", "url": "url" }
}
```
- 공유 시트는 **앱마다 담는 자리가 다르다.** `url`이 아니라 `text`에 주소를 넣는 앱이 흔하다.
  → 세 자리를 **전부 훑어서** 주소를 찾아낼 것.
- 매니페스트와 아이콘은 **로그인 없이 받을 수 있어야 한다.** 브라우저가 쿠키 없이
  가져가므로 인증으로 막으면 설치가 안 되고, 그러면 공유 시트에도 안 뜬다.

> **가장 싼 해법을 먼저 찾아라.** 우리는 "첨부 ID로 원본을 역산"하느라 며칠을 썼는데,
> 답은 **"공유할 때 메시지 칸에 링크를 같이 붙여넣으면 된다"**였다. 스캐너는 원래부터
> 본문의 URL을 읽고 있었다. 기술로 풀기 전에 **사용자가 한 동작 더 하면 끝나는지** 보라.

---

## 2. 식별자는 여러 체계가 섞여 있다

**원칙.** ID를 다른 ID로 변환하기 전에 **모양을 검사**하고, 변환 후 **결과를 검증**한다.

**왜.** 인스타 게시물 코드는 미디어 pk(19자리)의 base64다. 그래서 DM 첨부의
`asset_id`로 원본을 역산할 수 있다고 판단했는데 — 그 값은 **17자리로 다른 체계**였다.
검증 없이 저장했다면 `/p/BATXplxuoj/` 같은 **죽은 주소가 DB에 쌓였을 것이다.**

**어떻게.**
```ts
// ① 모양 검사 — pk 가 아니면 아예 시도하지 않는다 (헛된 네트워크 호출도 막는다)
if (!/^\d{18,20}$/u.test(id)) return null;

// ② 결과 검증 — 실제로 쓸 수 있을 때만 인정한다
const guess = await resolve(attachment);
const slides = guess ? await fetchSlides(guess) : [];
if (guess && slides.length > 1) save(guess);   // 검증 통과분만 저장
```

**교훈.** 내 검증이 틀렸던 이유: **결과물(게시물)에서 뽑은 ID로 테스트**했다.
실제 입력(DM 첨부)으로 시험하지 않으면 "된다"는 착각을 확인만 하게 된다.

---

## 3. URL 정규화는 저장 시점에

**원칙.** 같은 리소스에 여러 URL 형태가 존재한다. **받는 즉시 표준형으로 줄여 저장한다.**

**왜 — 가장 크게 당한 것.** 인스타 "링크 복사"는 `/<사용자명>/p/<코드>/`를 준다.
이 경로로 임베드를 요청하면 **데이터 없는 껍데기(658KB)**가 오고, 표준형
`/p/<코드>/`는 **정상(358KB)**이 온다. 로컬에서 손으로 테스트할 땐 줄인 주소만 써서
통과했고, **배포 환경에서만 실패**했다. 원인을 찾는 데 가장 오래 걸렸다.

**어떻게.**
```ts
// 사용자명·추적 파라미터를 떼고 타입+코드만 남긴다
const m = pathname.match(/(?:^|\/)(p|reel|reels|tv)\/([A-Za-z0-9_-]+)/iu);
return m ? `https://www.instagram.com/${m[1].toLowerCase()}/${m[2]}/` : null;
```
- 정규화를 **소비 시점**에 하면 이미 늦다. 저장된 값이 제각각이면 중복 판정도 깨진다.
- 테스트 케이스에 **지저분한 실제 형태**를 넣어라 (`?igsh=...`, 사용자명 포함, 대소문자).

---

## 4. 성격이 다른 주소를 한 필드에 담지 않는다

**원칙.** **영구 링크**와 **만료되는 CDN 주소**는 다른 필드다.
그리고 **라벨은 "어느 필드에 있었나"가 아니라 "어떤 값인가"로 정한다.**

**왜.** 이 프로젝트의 최초 버그가 이것이었다. CDN 이미지 주소가 "원본 링크"로 걸려
있어서, 며칠 뒤 서명이 만료되면 링크가 죽었다. 필드를 나눠 고쳤는데 — 이번엔
사용자가 "원래 있던 주소가 사라졌다"고 했다. **양쪽을 다 만족시키는 답이 라벨 규칙이다.**

```ts
export function outboundLink(sourceUrl, thumbnailUrl) {
  const href = sourceUrl || thumbnailUrl;
  if (!href) return null;
  // 값 자체가 만료 여부를 말해준다 → 어느 칸에 있든 오해할 일이 없다
  return { href, expiring: isMediaUrl(href) };
}
```

**UI에서도 자리를 나눠라.** 한 입력칸에 섞으면 사용자가 진짜 주소를 넣으려 할 때
기존 값을 지우는 손이 먼저 든다. "원본 링크" 칸과 "받은 주소(참고)" 표시를 분리한다.

---

## 5. 같은 URL도 UA에 따라 다른 것을 준다

**원칙.** 추정하지 말고 **실측**한다. 기능마다 정답이 반대일 수 있다.

**왜.** 인스타에서 실측한 결과:

| 목적 | 필요한 UA | 다른 UA면 |
|---|---|---|
| 임베드에서 캐러셀 JSON 받기 | **브라우저 UA** | 크롤러 UA → 껍데기 |
| 자식코드 → 부모 게시물 리다이렉트 | **비브라우저 UA** | 브라우저 UA → SPA 껍데기 200 |

정반대다. 한쪽에서 통한 UA를 다른 쪽에 그대로 쓰면 조용히 실패한다.

**어떻게.** 각 호출마다 UA를 명시하고, **왜 그 UA인지 주석**을 남겨라.
안 그러면 다음 사람(=3개월 뒤의 나)이 "통일하자"며 고쳐 놓고 깨뜨린다.

---

## 6. 프로덕션에서만 실패하면 — 일회용 진단 엔드포인트

**원칙.** 로그를 읽을 수 없거나 재현이 안 되면, **서버가 실제로 무엇을 받는지
측정하는 라우트를 배포**한다. 확인 즉시 지운다.

**왜.** 이번 작업에서 가장 강력한 도구였다. "Vercel이 차단당한 것 같다"는 추정이
완전히 틀렸음을 10분 만에 밝혔고(차단 없었음, 원인은 URL 형태), 비전 추출이
"고장났다"는 보고도 서버에선 멀쩡함을 증명해 범위를 좁혔다.

**어떻게.**
```ts
// /api/diag-xxx — 입력을 받지 않는다(하드코딩). SSRF 여지를 아예 없앤다.
export async function GET() {
  const res = await fetch(TARGET, { headers: { 'user-agent': UA } });
  const body = await res.text();
  return NextResponse.json({
    status: res.status, finalUrl: res.url, bytes: body.length,
    marker: (body.match(/display_url/g) ?? []).length,   // 있어야 할 것의 개수
    loginWall: /accounts\/login/i.test(body),
  });
}
```
지켜야 할 선:
- **입력 없음** — 사용자가 URL을 넘길 수 있으면 SSRF 통로가 된다
- **개인정보 없음** — 개수·길이·존재 여부만. 본문은 담지 않는다
- **타임아웃 필수** — 이미지 다운로드가 함수 시간을 먹어 504가 났었다. `HEAD` + `AbortSignal.timeout()`
- **확인 후 즉시 제거하고, 제거를 검증**한다

---

## 7. LLM 비전으로 이미지에서 정보 뽑기

큐레이션 게시물은 정보가 **이미지 안에 글자로** 그려져 있어 텍스트로는 없다.
여기가 LLM이 판을 뒤집는 지점이다. 단, 함정이 많다.

### 이미지를 URL로 넘기지 마라
```ts
// ✗ LLM 서버가 대신 받아온다 → 서명된 CDN은 외부 페처에게 안 내준다
source = { type: 'url', url: imageUrl };

// ✓ 우리 서버가 받아서 실어 보낸다 (호스트는 못 박아 SSRF 차단)
const res = await fetch(imageUrl, { headers: { 'user-agent': 'myapp/1.0' } });
source = { type: 'base64', media_type: contentType, data: base64 };
```
만료된 CDN 주소는 이미지 대신 **HTML을 돌려준다.** `content-type`을 반드시 확인하면
"링크가 만료됐어요"라고 정확히 말해줄 수 있다.

### 구조화 출력 스키마는 단순하게
`enum` + `nullable` 조합 같은 가장자리를 피하고 **전부 문자열로 받은 뒤 코드에서 검증**한다.
스키마가 복잡할수록 조용히 실패할 여지가 늘어난다.
```ts
kind: z.string(),      // 'place' | 'item' | '' — 검증은 코드에서
category: z.string(),  // 허용 목록 밖이면 버린다
```

### 금지 규칙을 명시하고, 코드로 이중 방어
가게 카드에는 상호명보다 **주소·전화번호가 더 크게** 박혀 있다. 그대로 옮겨 적으면
목록이 주소로 채워진다.
```
프롬프트: "name 에는 가게 이름이나 제품 이름만. 주소·전화번호·영업시간은 memo 로.
          못 찾으면 비워두고 지어내지 않는다."
코드:     주소 꼴이면 이름으로 쓰지 않는다 (지시를 어겨도 걸린다)
```

### 여러 장이면 다수결
한 게시물은 대개 한 종류다. 장마다 판단하게 하고 **다수결로 모으면** 한 장이 튀어도
전체가 흔들리지 않는다.

### 실패 사유를 감추지 마라
`"이미지를 읽지 못했어요"` 한 줄로는 아무것도 못 고친다. 실제 오류 메시지를
붙여서 내보내라. 알림창보다 **화면에 남기는 편**이 낫다 — 닫으면 사라지니까.

### 비용
슬라이드 1장 ≈ 10~30원, 10장짜리 게시물 ≈ 100~300원. 전사 작업에 가까우므로
**effort는 낮게**. 줄마다 병렬로 부르면 전체 시간이 한 장 읽는 시간과 같아진다.

---

## 8. 자동 채움 UX 원칙

**이 다섯 줄이 전부다.**

1. **초기값일 뿐이다.** 틀려도 사용자가 한 번 탭해서 고치면 그만.
2. **확신 없으면 비워둔다.** 엉뚱한 값은 빈 값보다 나쁘다 — 고치는 손이 더 든다.
3. **사용자가 적은 값은 절대 덮지 않는다.** 한 번 덮이면 기능을 끄고 싶어진다.
   - 예외: `jomalonelondon` 같은 **자리표시자**(계정 핸들 꼴)는 교체해도 된다
4. **짝짓기는 개수가 맞을 때만.** 애매하면 짝짓지 말고 사용자가 고르게 한다.
   - 다만 "표지 + 항목들 + 아웃트로" 구성은 흔하므로 **1~2장 어긋남은 표지 몫**으로 보정
5. **자동이 실패해도 수동 경로를 남긴다.** 버튼은 지우지 말 것.

---

## 9. 분류 체계 설계

**원칙.** 넓은 분류 하나는 쓸모가 없다. 단, **세분화할 때 기존 데이터를 고아로 만들지 마라.**

**왜.** "서울"에 13건이 쌓이면 *오늘 홍대에 있는 내가 무엇을 갈 수 있는지* 알 수 없다.
그렇다고 서울을 없애고 권역만 남기면, 예전에 "서울"로 저장한 13건이 **어느 칸에도
안 잡혀 사라진다.**

**어떻게 — 상위 선택 시 하위 포함.**
```ts
export function regionScope(region: string): readonly string[] {
  if (region === '서울') return [region, ...SEOUL_AREAS];  // 예전 값 + 권역 전부
  return [region];
}
// 필터: { region: { in: [...regionScope(region)] } }
```

곁들여:
- 분류는 **행정구역이 아니라 사용 맥락**으로 나눈다 ("간 김에 들를 수 있나")
- 정규화 테이블을 두지 말고 **문자열 그대로 저장**한다. 프리셋에서 항목을 지워도
  기존 데이터는 남고 필터에서만 사라진다 — 개인 취향은 계속 바뀌기 때문
- 자동 채움이 뱉은 값이 **선택 목록에 있는지 테스트**로 묶어라. 목록에 없는 값이
  저장되면 UI에서 고를 수 없는 유령이 된다

---

## 10. 스키마 마이그레이션에서 밟은 지뢰

**NOT NULL 해제 순서.** 컬럼을 NULL로 만들기 **전에** 제약을 먼저 풀어야 한다.
```sql
ALTER TABLE t ALTER COLUMN c DROP NOT NULL;   -- 먼저
UPDATE t SET c = NULL WHERE ...;              -- 그다음
```

**유니크 키에 nullable 컬럼을 넣지 마라.** Postgres는 NULL을 서로 다르게 보므로
중복이 막히지 않는다. → **전용 키 컬럼**을 만들고 기존 값으로 백필한다.
```sql
ALTER TABLE t ADD COLUMN "dedupeKey" TEXT;
UPDATE t SET "dedupeKey" = "sourceUrl";        -- 기존 판정 기준을 그대로 옮긴다
ALTER TABLE t ALTER COLUMN "dedupeKey" SET NOT NULL;
```
중복 판정 기준이 바뀌면 **이미 수집한 것이 다시 들어온다.** 값을 그대로 옮기는 게 핵심.

**데이터 정리는 좁게.** "CDN 주소를 옮긴다"면 **호스트를 못 박아라.**
사용자가 직접 적은 아무 링크까지 쓸어가면 안 된다.

---

## 11. 작업 습관 — 이번에 대가를 치른 것들

**앵커 치환은 단계마다 검증한다.** 여러 replace를 묶어 돌리고 "전체가 바뀌었나"만
보면, 일부만 적용된 **반쪽 상태**로 넘어간다. taxonomy를 고칠 때 실제로 그랬다.
```js
const swap = (from, to) => {
  if (!s.includes(from)) throw new Error('앵커 없음: ' + from.slice(0, 70));
  s = s.replace(from, to);
};
```

**줄바꿈을 정규화하고 다뤄라.** 저장소는 LF인데 체크아웃에서 CRLF가 섞이면
여러 줄 치환이 전부 빗나간다. `replace(/\r\n/g, '\n')` → 수정 → LF로 저장.

**배포 상태는 공개 API로 확인할 수 있다.** CLI 없이도:
```
GET api.github.com/repos/<owner>/<repo>/commits/<sha>/status  → Vercel 상태
```

**검증한 것과 안 한 것을 구분해 보고하라.** "테스트·빌드는 통과, 로그인 뒤 경로는
확인 못 함"처럼. 로컬에 DB나 API 키가 없으면 **증명할 수 없는 구간이 반드시 생긴다.**

---

## 12. 다른 플랫폼으로 옮길 때 체크리스트

새 플랫폼(유튜브·틱톡·스레드·네이버 등)에 붙일 때 **이 순서로 실측**하라.
추정으로 시작하면 이번처럼 며칠을 쓴다.

- [ ] **공유 시트가 무엇을 주는가** — `url`/`text`/`title` 중 어디에 주소가 들어오나
- [ ] **원본 주소의 형태가 몇 가지인가** — 단축 URL, 사용자명 포함, 추적 파라미터
- [ ] **로그인 없이 받을 수 있는 것** — oEmbed / embed 페이지 / og 태그 중 무엇이 살아있나
- [ ] **UA별 응답 차이** — 기능마다 다를 수 있으니 각각 시험
- [ ] **미디어 주소가 만료되는가** — 만료되면 **받아서 자체 저장**해야 한다
- [ ] **웹훅/DM이 주는 payload가 콘텐츠 종류마다 다른가** — 종류별로 한 번씩 보내보기
- [ ] **공식 API로 풀리는가** — 검수·토큰이 필요한지, 그 비용이 값어치를 하는지
- [ ] **비공식 경로를 쓴다면 폴백이 있는가** — 실패 시 조용히 물러나고 수동 경로가 남아야 한다

> 비공식 경로(embed 스크래핑 등)는 **언제든 깨진다는 전제**로 짠다.
> 전부 빈 배열로 폴백하고, 사용자에게는 수동 경로를 남겨라.
> 그러면 플랫폼이 바꿔도 앱이 죽지 않고 조금 불편해질 뿐이다.

---

## 부록 — 이 프로젝트의 최종 구조

```
수집          분류              저장
──────────────────────────────────────────────
공유 시트  ┐
웹훅/DM    ├→  "분류해주세요"  →  가고싶어요
수동 입력  ┘    (수신함)          배달
                                 사고싶어요

자동 채움 파이프라인
  원본 주소 → 임베드에서 슬라이드 N장
            → 항목 ↔ 슬라이드 짝짓기(개수 맞을 때만)
            → 장마다 LLM 비전: 이름·정보·종류·분류
            → 다수결로 종류·분류 확정
            → 사용자 확인 후 저장 (항목별 사진 개별 저장)
```

**핵심 한 줄.** 플랫폼이 주는 데이터는 불완전하다는 전제로 짜고,
**사용자가 한 동작 더 하면 완전해지는 길**을 항상 열어둬라.
